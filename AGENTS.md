# Markov Process — Workspace

This directory (`/home/amirelite_ai/markovProcessTopic`) is the dedicated
working folder for the **"Markov Process"** Telegram topic in the Hermes
group.

## Conventions
- All files, notes, scripts, and artifacts related to Markov-process work
  (state-transition matrices, chain simulations, notebooks, references) live here.
- When working in the "Markov Process" topic, operate from this directory by
  default unless the user specifies otherwise.
- Keep long-running scratch in subfolders (e.g. `notes/`, `code/`, `data/`).
